export type Project = {
  projectName: string;
  description: string;
  projectUrl: string;
  myContribution: string;
  gitHubUrl?: string;
  img: string;
};

export type ImageSource = { src: string; alt: string };
