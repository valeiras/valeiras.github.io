import React from "react";
import styled from "styled-components";

export default function SuspensePlaceholder(): React.JSX.Element {
  return <Wrapper></Wrapper>;
}

const Wrapper = styled.div`
  width: 100vw;
  height: 100vh;
`;
